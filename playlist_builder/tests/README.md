# Host regression tests

These tests compile the production plugin with an in-memory device and filesystem. Run the commands in the repository README from the repository root.

The fake plugin.h is for host tests only; device builds must use Rockbox headers.
